# ``IcecastKitCLI``

Command-line tool for streaming audio to Icecast and SHOUTcast servers.

## Overview

`icecast-cli` provides commands for streaming, probing, relaying, testing connections, and querying server information. See the IcecastKit documentation for the full CLI reference.
